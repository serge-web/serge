
/** 
 * Move the specified child asset to the root level for that force
 */
const groupMoveToRoot = (uniqid: string, forces: any): any => {
  console.log('moving to root', uniqid, forces && forces.length)
  return forces
}

export default groupMoveToRoot
