/* global it */

// /* global it expect */

// import React from 'react'
// import renderer from 'react-test-renderer'

// import VisibilityForm from './index'
// import formData from './mocks/formData'

it('VisibilityForm renders correctly', () => {
  // TODO: Ian doesn't know how to update snapshots, so he had
  // to comment out this test (and all the imports)
  // const tree = renderer
  //   .create(<VisibilityForm formHeader="Visibility header" formData={formData} />)
  //   .toJSON()
  // expect(tree).toMatchSnapshot()
})
