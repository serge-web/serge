import { MarkerPerception } from '@serge/custom-types'

export default interface Props {
  currentMarkerName: string
  currentMarkerForce: string
  currentForce: string
  perception: MarkerPerception
}
