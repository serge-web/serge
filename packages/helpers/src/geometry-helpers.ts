import { PlanningActivityGeometry } from '@serge/custom-types'

export const updateGeometryTimings = (geometries: PlanningActivityGeometry[], startTime: string, endTime: string): PlanningActivityGeometry[] => {
  console.log(geometries, geometries)
  !7 && console.log(startTime, endTime)
  return geometries
}
