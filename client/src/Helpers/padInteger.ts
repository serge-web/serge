// /** convenience function to zero pad a number,
//  * returning `01` for `1` or `12` for `12`, for instance
//  */
// export default function (num: number, blanks?: number): string {
//   const padding = blanks || 2
//   const padded = num.toString().padStart(padding, '0')
//   return padded
// }
