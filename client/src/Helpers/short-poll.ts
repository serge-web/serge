// const poll = (fn: any, timeout: any): any => {
//   const promise = (resolve: any) => {
//     setTimeout(() => {
//       fn().then(resolve(true))
//     }, timeout)
//   }
//   return new Promise(promise)
// }

// export default poll
